# Ensure Windows compatibility and prevent file locking issues
# Enable strict peer dependencies to prevent version conflicts
strict-peer-deps = true
# Optimize for Windows performance
prefer-offline = true
progress = false