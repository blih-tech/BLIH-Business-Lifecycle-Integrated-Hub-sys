import { PublicJobsPageClient } from '@/features/careers/components/public-jobs-page-client';

export const metadata = {
  title: 'Open Positions â€” BLIH',
  description:
    'Explore active job opportunities at BLIH. Browse published roles and apply today.',
};

export default function PublicJobsPage() {
  return <PublicJobsPageClient />;
}
