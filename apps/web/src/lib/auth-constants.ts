/** First-party snapshot cookie (local split-origin dev only). Not the Keycloak access token. */
export const LOCAL_SESSION_COOKIE = 'blih_local_session';

/** sessionStorage key for DevTools inspection of last /auth/me snapshot. */
export const LOCAL_DEV_SESSION_STORAGE_KEY = 'blih_dev_auth_me';

/** Optional manual Bearer token for API calls (e.g. Postman-copied JWT). */
export const LOCAL_STORAGE_ACCESS_KEY = 'access_token';
