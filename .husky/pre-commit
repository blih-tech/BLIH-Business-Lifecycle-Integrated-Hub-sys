#!/usr/bin/env sh
set -e

echo "🔍 Running lint-staged checks..."
# ⚡ Only 1–2 concurrent tasks, max 3000 characters per chunk
npx lint-staged --concurrent 1 --max-arg-length 3000

echo "✅ Pre-commit checks passed"